Azure Table Storage integration test example.

## Prerequisite
1. Create a storage account.
2. Replace `<connection string>` in `EmployeeRepositoryTests.cs` with the storage account's connection string.